
  # Responsive Homepage with Contact Form

  This is a code bundle for Responsive Homepage with Contact Form. The original project is available at https://www.figma.com/design/cSLpooibZB0Uh2flQ5AePT/Responsive-Homepage-with-Contact-Form.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  